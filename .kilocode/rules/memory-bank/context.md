# Current Context

- 2026-05-02: Pipecat live voice is running in frontend-routed mode. The Pipecat backend performs VAD and OpenAI STT, then the chat UI sends final user transcripts through `/api/chat` and `/api/tts`.
- 2026-05-02: Fixed Pipecat live voice transcript handling so final STT payloads are trimmed, deduped, and flushed into the chat response path after a short pause.
- 2026-05-02: Fixed the live voice waveform display so Pipecat audio levels spread across the full visualizer instead of scrolling into a bunched cluster.
- 2026-05-02: Kept the voice visualizer at its original compact size, while distributing level movement across that compact spectrum.
- 2026-05-02: Added `transport.output()` to the frontend-routed Pipecat pipeline so RTVI transcript messages can reach the browser without re-enabling Pipecat's internal LLM/TTS path.
- 2026-05-02: Hardened chat timestamp rendering and persisted store hydration so object-shaped timestamps from live voice/message updates cannot crash the chat UI.
- 2026-05-02: Hardened chat message rendering for object-shaped message ids, roles, and content so live voice message payloads cannot crash the wrapper before timestamp rendering.
- 2026-05-02: Removed message-id coercion from the React render key; the message list now uses index keys in the chat render boundary to avoid object-to-primitive crashes from malformed live voice message ids.
- 2026-05-02: Added store-level normalization for new, updated, and persisted chat messages/threads so malformed live voice payload fields are cleaned before they persist or render.
- 2026-05-02: Removed SVG avatar icons from the volatile chat message and typing rows after malformed live voice data triggered object-to-primitive crashes in SVG path rendering.
- 2026-05-02: Fixed main app horizontal overflow by sizing the fixed-sidebar content panel to `calc(100% - sidebarWidth)` instead of shifting a full-width panel to the right.
- 2026-05-02: Windows crash logs showed repeated `chrome.exe` access violations while Pipecat WebRTC was active; mitigated by forcing the SmallWebRTC client offer to audio plus inactive video, pausing the galaxy canvas during live voice, throttling Pipecat level UI updates, and revoking replaced TTS object URLs.
- 2026-05-02: After the WebRTC mitigation, long live tests no longer showed new remote camera-track endings but still hit object-to-primitive errors in the chat message render loop; simplified message rows to plain divs/text controls with no per-message avatars, SVG action icons, or AnimatePresence/motion wrappers.
- 2026-05-03: Moved chat jump controls out of the message list and into a floating in-chat overlay pinned above the input area, so Top/Bottom stays visible while scrolling long chats; Bottom scrolls the pane directly to its `scrollHeight`.
- 2026-05-03: Restored compact per-assistant-message action icons using stable Heroicons while keeping store/message normalization that prevents malformed live voice payloads from crashing render.
- 2026-05-03: Adjusted Top/Bottom to use the opaque far-left chat-pane rail treatment while remaining pinned to the visible pane during message scrolling.
- 2026-05-03: Implemented real image attachments: image picker now creates pending clickable thumbnails, sends image-only prompts with a default "Describe this image in great detail." instruction, renders sent clickable thumbnails under the user message, persists attachment metadata/data URLs, and forwards images through `/api/chat` as OpenAI-style `image_url` content parts.
- 2026-05-03: User reported image thumbnails did not expand and the active local vLLM endpoint rejected image prompts with "at most zero images may be provided." Confirmed `/v1/models` reports `AEON-7/Qwen3.6-35B-A3B-heretic-NVFP4` on port 8003; current served instance is enforcing zero images even though UVB docs plan Qwen-VL/vision separately.
- 2026-05-03: Replaced data-URL thumbnail links with an in-app lightbox, moved Top/Bottom into the left thread-sidebar rail, hardened scroll commands with direct scroll container updates, and added a specific vision-model-needed assistant message when the local server rejects image content with zero-image support.
- 2026-05-03: User disliked the sidebar rail Top/Bottom placement. Moved the jump control back to the chat pane's far-left edge as a translucent vertical arrow tab, pinned to the visible chat pane rather than inside the message flow. Top targets a real `messagesTopRef` sentinel; Bottom targets `messagesEndRef`/scrollHeight. Docker commit attempt for vLLM cache was interrupted and left the container paused; unpaused `aria-vllm-qwen36-35b-a3b-heretic` and verified `/v1/models` responds again.
- 2026-05-02: Live Pipecat transcript flushing now waits longer after final/stop events and preserves pending transcript fragments that arrive while a response is in flight, retrying after the current response completes instead of dropping the second fragment.
- 2026-05-06: Configured the local Telegram bridge with the Sophia Knight bot token in `.env.local` and locked it to the user's Telegram chat id via `TELEGRAM_ALLOWED_CHAT_ID`.
- 2026-05-06: Started the local Telegram polling worker after confirming UVB `/api/health`, `/api/chat`, and direct Telegram `sendMessage` were healthy; removed Telegram Markdown parse mode and added per-message worker logging in `scripts/telegram-worker.mjs`.
- 2026-05-06: Extended `scripts/telegram-worker.mjs` so Sophia can send Kokoro TTS replies to Telegram via `/api/tts`/`sendAudio`, route Telegram voice/audio through `/api/stt`, and download Telegram photos or image documents into `/api/chat` as OpenAI-style `image_url` content. Added local env toggles for text replies, TTS replies, TTS voice, and TTS max chars.
- 2026-05-06: Fixed Telegram Kokoro replies so long Sophia responses are split into sequential audio parts instead of truncating at the old 1200-character limit. `.env.local` now uses `TELEGRAM_TTS_CHUNK_CHARS=2200` and `TELEGRAM_TTS_MAX_PARTS=6`.
- 2026-05-06: Raised Sophia Telegram Kokoro audio chunking to a roughly three-minute target by changing the default/local `TELEGRAM_TTS_CHUNK_CHARS` from `2200` to `4200`; multi-part audio remains as a fallback for very long responses.
- 2026-05-06: Fixed Sophia Telegram text truncation by replacing `answer.slice(0, 3900)` with ordered text-message chunking (`TELEGRAM_TEXT_CHUNK_CHARS`, default `3600`) so the text transcript matches the full audio response.
- 2026-05-06: Added Telegram text-document routing for Sophia. `.txt`, markdown, JSON, CSV, logs, and common code/config documents are downloaded, wrapped with filename/caption delimiters, capped by `TELEGRAM_DOCUMENT_MAX_CHARS` (default `120000`), and sent through UVB chat instead of falling through as unsupported documents.
- 2026-05-06: Added first-pass Telegram video routing for Sophia. Telegram videos, video notes, animations, and video documents are downloaded under `TELEGRAM_VIDEO_MAX_MB`, processed with local `ffmpeg`, transcribed via `/api/stt`, paired with one extracted reference frame when possible, and routed through UVB chat.
- 2026-05-06: Upgraded Sophia Telegram video routing from one thumbnail to a multi-frame storyboard. The worker now probes duration with `ffprobe`, samples ordered frames across the clip (`TELEGRAM_VIDEO_FRAME_COUNT`, default `6`, capped at `12`), scales them via `TELEGRAM_VIDEO_FRAME_MAX_WIDTH`, and sends the timeline frames plus audio transcript to UVB chat.
- 2026-05-06: Raised Sophia Telegram video local routing limit from `80` MB to `500` MB via `TELEGRAM_VIDEO_MAX_MB` so larger user videos can be processed locally.
- 2026-05-06: Confirmed Telegram hosted Bot API blocks `getFile` downloads above 20 MB before UVB's local `TELEGRAM_VIDEO_MAX_MB` guardrail. Added `TELEGRAM_API_ORIGIN`, `TELEGRAM_FILE_ORIGIN`, and clearer large-file errors so Sophia can use a local Telegram Bot API server for large videos later.
- 2026-05-06: Enabled the live vLLM container's multimodal prompt limit for video by recreating `aria-vllm-qwen36-35b-a3b-heretic` with `--limit-mm-per-prompt {"image":1,"video":1}`; the previous container was preserved as a timestamped rollback copy and `/v1/models` came back healthy on port 8003.
- 2026-05-06: Added first real local UVB web video analysis in Media Studio. `/api/media/video` accepts local uploads, extracts audio with `ffmpeg`, transcribes through Faster Whisper, samples storyboard frames plus a one-image contact sheet for the one-image vLLM path, asks Sophia for a video analysis, and returns transcript/storyboard data to the UI.
- 2026-05-06: Added local UVB chat ingestion for text-like document attachments. The chat file picker now reads `.txt`, markdown, JSON, CSV/TSV, logs, config files, and common code files into wrapped prompt blocks with a 512 KB file guard and 120k character cap; binary/PDF/docx extraction remains a later parity step.
- 2026-05-06: Completed the next local document parity step by adding `/api/documents/extract` for PDF and DOCX extraction with `pdf-parse` and `mammoth`. Local chat now sends PDF/DOCX files through server-side text extraction, wraps the extracted text into the prompt, caps extracted text at 120k characters, and keeps unsupported binary files as explicit notes.
- 2026-05-06: Polished local document extraction with PPTX and XLSX support via OOXML zip/XML text extraction. Local chat now accepts `.pptx` and `.xlsx` through the same file picker and routes extracted slide/sheet text into Sophia prompts.
- 2026-05-06: Replaced the Memory Bank demo page with a live local memory cockpit. It now derives conversation memories from real chat threads, lets the user create/delete pinned manual memories in localStorage, supports search/filter/export, and accurately labels embeddings/RAG as future work rather than pretending they are active.
- 2026-05-06: Added local identity/profile settings for the user and Sophia. Settings can now save user name/email/portrait, Sophia name/subtitle/portrait, and app name to localStorage; Header, Sidebar, and Chat consume those identity settings live via a browser event.
- 2026-05-06: Added local Telegram chat logging into UVB chat history. The Telegram worker now posts completed turns to `/api/telegram/log`, the API stores them in `.uvb/telegram-chats.json`, and the Chat page syncs those logs into normal chat threads marked with a Telegram badge.
- 2026-05-06: Replaced the Voice Analysis demo page with a real local workflow. It now supports microphone recording, audio upload, playback, browser-side waveform/energy metrics, and local transcription through `/api/stt` using configured voice settings.
- 2026-05-06: Added a Voice Clone Lab section to Voice Analysis. It supports local clone profiles in localStorage, provider targets for Kokoro/Chatterbox/MOSS/VibeVoice/RVC/custom, reference sample attachment from current audio, readiness metadata, tuning controls, tags/notes, save/delete/load, and test rendering through the current `/api/tts` endpoint.
- 2026-05-06: Added Sophia Agent Tools groundwork in Settings. The new Agent Tools tab stores local permissions for browser use, web research, local computer use, coding tasks, terminal/file/git/network access, approval mode, workspace/domain/path scope, audit logging, and coding provider fallback options including local UVB, Kilo Code Gateway (`https://api.kilo.ai/api/gateway`), and custom OpenAI-compatible endpoints.
- 2026-05-06: Ran a UI/UX polish sweep across active sections. Media Studio image analysis now uses the real `/api/chat` vision path instead of demo results, and Podcast Studio now has a working recording timer, controlled mix settings, and integration with saved Voice Clone Lab profiles.
- 2026-05-06: Added an Infrastructure Priority Stack to Settings > Agent Tools so Sophia's browser/computer-use/coding roadmap is visible in-app. It prioritizes approval/audit queues, browser-use runtime, coding agent adapters, workspace patch sandboxing, Windows computer-use bridge, memory retrieval, and provider health/fallbacks.
- 2026-05-06: Added a research-backed Voice Model Catalog for STT/TTS provider planning. Voice Analysis now shows a State-of-the-Art Voice Stack with wired/candidate/future lanes, local Z-drive model paths, and provider notes for Faster Whisper, Parakeet, Canary, VibeVoice ASR/TTS/Realtime, Kokoro, Chatterbox Turbo/Multilingual, F5, CosyVoice2, Spark/GLM/Qwen local experiments, and future expressive models.
- 2026-05-06: Upgraded Podcast Studio with a synthesis engine selector and render mode controls. VibeVoice 1.5B is the default long-form podcast lane, VibeVoice Realtime is the live streaming lane, Chatterbox Turbo is the near-term low-latency clone lane, and Kokoro remains the fast fallback.
- 2026-05-06: Added a localStorage safety guard for Voice Clone Lab reference samples so oversized clips are rejected before bloating browser storage.
- 2026-05-06: Folded the latest voice-agent watchlist into the Voice Model Catalog. Added MOSS-TTS Realtime/Nano/TTSD, Fish Audio S2 Pro, whisper.cpp streaming, XTTS-v2, Pipecat, LiveKit Agents, and Vocode lanes; Pipecat remains the primary orchestration path, MOSS/Fish are high-priority TTS benchmark candidates, and LiveKit/Vocode are watch/fallback frameworks rather than rebuild targets.
- 2026-05-06: Added `/api/voice/stack` so UVB can inspect the Voice Model Catalog against real local files and sidecar repos. Settings > Voice now shows a live Voice Stack Infrastructure panel, and `scripts/bootstrap-voice-stack.ps1` / `bun run voice:bootstrap` clone priority voice sidecar repositories into `Z:\Models\_uvb-sidecars` without tracking them in Git.
- 2026-05-06: Ran another no-demo UI/UX pass. Added persisted UI appearance/privacy/notification settings, live theme/accent/particle application, a filtered command palette, functional header search/alerts/profile buttons, real image preview after Media Studio image analysis, and removed the old fake chat fallback response table that contained bogus RAG/vector claims.
- 2026-05-06: Upgraded Podcast Studio from a timer-only recording surface to real local microphone session capture with pause/resume, playback, and export. Podcast seats can select saved Voice Clone Lab profiles, and clone profile saves now dispatch a local update event so Podcast refreshes without a browser reload.
- 2026-05-06: Added Settings > Agent Tools Capability Readiness so Sophia's web research, browser use, local coding, Kilo Gateway, computer use, and memory retrieval status is explicit. The UI now distinguishes configured/staged/partial capabilities instead of implying the execution worker exists before it is wired.
- 2026-05-06: Completed manual Memory Bank CRUD by adding edit/update support for local pinned memories. Manual memories now support create, search, edit, delete, and export; chat-thread memories remain derived/read-only.
- 2026-05-06: Added first real Sophia agent job infrastructure. `/api/agent/jobs` stores supervised deep-research, browser-use, local-coding, and computer-use jobs in `.uvb/agent-jobs.json` with approval status, execution plans, provider/workspace/domain metadata, artifacts, and audit entries. Settings > Agent Tools can create/refresh/approve/cancel these jobs, and Telegram commands `/research`, `/browser`, `/code`, and `/computer` queue the same supervised jobs instead of pretending autonomous execution is already live.
- 2026-05-06: Added registry-agnostic Agent Skill Intake. `/api/agent/skills` exposes curated discovery sources (SkillsGate, SkillsMD, Open Agent Skills, askill, Invoked, Skilldex) and stores staged skill candidates in `.uvb/agent-skills.json` with source URL, trust tier, approve/block status, optional SKILL.md content, and a local risk scan for shell/network/secret patterns. Settings > Agent Tools now has a skill intake UI so external skills are metadata-reviewed before any future installer/executor can use them.
- 2026-05-06: Added local account profile groundwork for future Cloudflare/remote/Telegram multi-user access. `/api/profiles` stores profiles in `.uvb/user-profiles.json` with display name, username, email, role, Telegram chat id, allowed remote domains such as daplab.net/tacimpulse.net, access modes, notes, and PBKDF2 password hashes rather than plaintext. Settings > Profile now has an Account Profiles section for creating/editing/deleting separate users.
- 2026-05-06: Added auth-provider readiness for real Google login and passkeys. `/api/auth/readiness` reports local-password, Google OIDC callback setup, and WebAuthn/passkey RP/origin setup from env (`UVB_GOOGLE_CLIENT_ID`, `UVB_GOOGLE_CLIENT_SECRET`, `UVB_PASSKEY_RP_ID`, `UVB_PASSKEY_ORIGIN`). Account profiles now store enabled auth providers, optional Google subject id, and passkey credential counts without storing plaintext passwords.
- 2026-05-06: Added Agent Job cleanup support. `/api/agent/jobs` now supports deleting completed/cancelled job records, and Settings > Agent Tools shows a Delete action for non-pending jobs so smoke tests and stale supervised work packets do not clutter the queue.
- 2026-05-06: Added ComfyUI image generation groundwork. Media Studio now has an Image Generation tab that stores ComfyUI endpoint/workflow settings in localStorage, checks `/system_stats`, and queues configured API workflows through `/api/image/comfy` with prompt/negative-prompt node substitution. `.env.example` now includes `UVB_COMFYUI_URL`.
- 2026-05-06: Added local coder handoff settings in Agent Tools. Approved coding jobs can now be configured for VS Code, Kilo Code, Antigravity-style IDEs, Codex CLI, OpenHands, or custom launcher commands while still preserving workspace/approval guardrails for the future runner.
- 2026-05-06: Added Sophia avatar companion groundwork. The main app now mounts a configurable local browser overlay, Settings > Appearance can save avatar mode/style/mood/position/asset/runtime settings, and `three` plus `@pixiv/three-vrm` are installed for the VRM runtime lane.
- 2026-05-06: Added Memory Bank Alias Rules. The Memory page now has an Alias Rules tab backed by `uvb:alias-rules` localStorage, with defaults for Jusstin, Codee, and Butt Stuff. Chat, `/api/chat`, `/api/stt`, and voice sidecars inject/apply those rules so STT, local chat, voice replies, and assistant output normalize canonical names/phrases.
- 2026-05-06: Cleared a stale Next dev overlay after the alias JSX arrow parse fix by restarting the local dev server and clearing only generated `.next` cache. Also hardened realtime voice cleanup so failed Pipecat/WebSocket starts tear down stale streams, sockets, audio elements, timers, and client refs; Pipecat transcripts now pass through Alias Rules before display/turn handling.
- 2026-05-06: Hardened Sophia's Telegram voice turn path after a voice clip appeared stuck at "Voice received. Transcribing locally...". The worker now logs timestamped download/STT/chat/TTS/send stages, applies UVB STT/chat/TTS timeouts, and sends Telegram progress pings during long local-model `/api/chat` turns instead of leaving the chat silent.
- 2026-05-06: Fixed the Telegram `.oga` voice STT hang in `/api/stt`. Direct Faster Whisper accepted `.oga` quickly, but forwarding the incoming Next `File` object could hang before reaching the container; the route now materializes uploads into a fresh Blob before proxying and applies `UVB_STT_TIMEOUT_MS` to the upstream STT call.
- 2026-05-06: Added a Telegram direct-STT path for voice/audio messages. Sophia's Telegram worker now sends downloaded voice files straight to the Faster Whisper OpenAI-compatible endpoint with model/language/punctuation/alias hints, bypassing the Next multipart proxy that can hang on Telegram `.oga` uploads.
- 2026-05-06: Fixed Telegram video analysis against the live one-image vLLM limit while preserving true video attempts. Telegram videos now send one `video_url` data URL to `/api/chat` when under `TELEGRAM_VIDEO_DIRECT_MAX_MB`, include transcript/storyboard metadata, and retry with a one-image storyboard contact sheet only if the local model rejects direct video.
- 2026-05-06: Upgraded true video routing to the local-media vLLM path. UVB now stages videos in `.uvb/model-media`, sends `file:///uvb-media/...` `video_url` parts for both Telegram and local Media Studio, keeps transcript/storyboard fallback evidence, and expects the vLLM container to be launched with `.uvb/model-media:/uvb-media` plus `--allowed-local-media-path /uvb-media`.
- 2026-05-06: Added `scripts/restart-vllm-local-media.ps1` and `bun run vllm:local-media` so the local-media vLLM launch is reproducible. The live `aria-vllm-qwen36-35b-a3b-heretic` container was recreated with the media bind mount and the previous container was preserved as a timestamped rollback.
