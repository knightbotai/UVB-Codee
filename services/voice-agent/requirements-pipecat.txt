pipecat-ai[webrtc]>=1.1.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Pipecat v1 + SmallWebRTC is the realtime transport layer for UVB live voice.
# Keep this separate from the baseline WebSocket requirements while both paths
# are available for fallback and diagnostics.
